---
title: Getting Started
description: Add a server to Console
---

In order to use the evals and review from Console, the first step is adding an MCP server.

## Adding a server

The following section describes how to add a server to Console and the different fields.

### Config

- URL (requiered): URL needs to be streamable http with `/mcp` endpoint. Console doesn't support legacy `/sse` (Server-side event transport)
- Name (optional) the server (default name is the hosturl)
- Headers (optional)

### Authentication

### Checking Tools

### Troubleshooting

- Try to reconnect
- Reach for support

### Limits
